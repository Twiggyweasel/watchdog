<% if @people.count == 0 %> 
    <div class="row v-c"> 
    <div class="col-xs-3"></div> 
        <div class="col-xs-6"> 
            <div class="m-y-2 card card-block text-xs-center" style="box-shadow: 0 0 6px rgba(0,0,0,.16),0 6px 12px rgba(0,0,0,.32);"> 
                <h5>No Active Records</h5> 
                <br /> 
                <%= link_to "Back", index_path, class:'color btn btn-primary m-b-2' %> 
            </div> 
        </div> 
    </div>            
<% else %> 
<div class="row vc"> 
        <section class="regular slider"> 
        <% @people.each do |person| %> 
            <div class="col-xs-3 m-t-2"> 
                <div class="height-fix card card-block text-xs-center" style="box-shadow: 0 0 6px rgba(0,0,0,.16),0 6px 12px rgba(0,0,0,.32);"> 
                    <% if person.image.blank? %> 
                        <%= image_tag "no-img.png", class:"img-fluid" %> 
                    <% else %> 
                        <%= image_tag person.image.url, class:"img-fluid" %> 
                    <% end %> 
                    <%= link_to "Full Record", person_path(person), class:'color btn btn-primary' %>                        
                    <h5 class="m-t-2">Person of Interest</h5> 
                    <p id="name"><%= person.name %></p> 
                    <p id="vitals"><%= person.height %> | <%= person.weight %> lb's</p> 
                    <h5>Reason for Issue</h5> 
                    <p id="reason"><%= person.reason_short %></p> 
                </div> 
            </div> 
        <% end %>      
        </section> 
</div>
<% end %> 

<!-- Calls Slick -->    
<script type="text/javascript"> 
    $(document).on('ready', function() { 
      $(".regular").slick({ 
        dots: true, 
        infinite: true, 
        slidesToShow: 3, 
        slidesToScroll: 3 
      }); 
      $(".center").slick({ 
        dots: true, 
        infinite: true, 
        centerMode: true, 
        slidesToShow: 3, 
        slidesToScroll: 3 
      }); 
      $(".variable").slick({ 
        dots: true, 
        infinite: true, 
        variableWidth: true 
      }); 
    }); 
</script>



<!--
<div class="col-sm-3 col-md-2 sidebar active-page">
    <h5 class="m-t-3 text-xs-left">Active Records</h5>
<ul class="center m-t-1 nav nav-pills text-xs-center" role="tablist">
    <% @people.each_with_index do |person, index| %>
    <% if index == 0 %>
  <li class="nav-item m-b-1">
    <a class="nav-link active" data-toggle="tab" href="#<%= person.id %><%= index %>" role="tab"><%= person.name %></a>
  </li>
  <% else %>
  <li class="nav-item m-b-1" style="margin-left: 0;">
    <a class="nav-link" data-toggle="tab" href="#<%= person.id %><%= index %>" role="tab"><%= person.name %></a>
  </li>
    <% end %>
    <% end %>  
</div>

<div class="row">
    <div class="tab-content m-t-2">
    <% @people.each_with_index do |person, index| %>
    <% if index == 0 %>        
    <div class="tab-pane active" id="<%= person.id %><%= index %>" role="tabpanel">
        <span>TEST <%= person.name %></span>
    </div>
    <% else %>
    <div class="tab-pane" id="<%= person.id %><%= index %>" role="tabpanel">
        <span>TEST <%= person.name %></span>
    </div>
    <% end %>
    <% end %>
</div> -->