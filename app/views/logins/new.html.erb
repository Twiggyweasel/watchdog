<!-- Login Screen               --*>
<*-- Compiled and Error Checked --*>
<*-- On 9/29/2016               --->
<div class="row v-c">
	<div class="center card card-block card-shadow text-xs-center" style="min-height:391px;">	
		<div class="container">
			<div class="left-login">
				<h2>Welcome to Watchdog</h2>
			</div>
			<div class="col-xs-7">
				<span class="right" style="margin-top: .25rem">
					<input type="checkbox" id='my-checkbox' name="my-checkbox" checked >
				</span>
			</div>
		</div>
		
		<div class="container">
		<%= form_tag '/login' do %>	
			<div class="m-t-2 row">
				<div class="right col-xs-4">          
					<%= image_tag "badge.png", class:"img-fluid m-x-auto d-block" %>
				</div>
					<div class="col-xs-8 m-b-2">         
					<div class="left m-y-2 p-t-1 input-group h-f-l" id="inAs">
						<span class=s-i>
							Signing in as, <%= @boardingUser.first_name %> <%= @boardingUser.last_name %>
						</span>  
					</div>
					<div class="left m-y-2 p-t-1 input-group" id="login" style="display: none;">
						<%= text_field_tag(:employee_num, nil, class: 'm-b-1 form-control', id: 'numpadButton', placeholder: "Employee Number")  %>
						<span class="input-group-btn">
							<button class="m-b-1 btn btn-default" id="numpadButton-btn" type="button"><i class="fa fa-th"></i></button>
						</span>  
					</div>
					<div class="left m-y-2 input-group">
						<%= password_field_tag :password, nil, :class => "m-b-1 form-control", :id => "numpadButton_2", placeholder: "Password"%>
						<span class="input-group-btn">
							<button class="m-b-1 btn btn-default" id="numpadButton-btn_2" type="button"><i class="fa fa-th"></i></button>
						</span>            
					</div>
					<div class="right-login">
						<%= submit_tag 'Login', class: 'color btn-size btn btn-primary' %>
					</div>					
				</div>
			</div>
		<% end %>
		</div>
	</div>
</div>
<script>
	$("[name='my-checkbox']").bootstrapSwitch();
	
	$(document).ready(function() {
		$('#login').hide();
		$('#numpadButton').attr('value', '<%= @boardingUser.employee_num %>');
    //set initial state.
    // $('#my-checkbox').val($(this).is(':checked'));
	
	$('input[name="my-checkbox"]').on('switchChange.bootstrapSwitch', function(event, state) {
  	// console.log(this); // DOM element
  	// console.log(event); // jQuery event
  	// console.log(state); // true | false
  	
  	if(state === false){
  		$('#inAs').hide();
  		$('#login').show();
  		$('#numpadButton').attr('value', '');
  	}
  	else {
  		$('#inAs').show();
  		$('#login').hide();
  		$('#numpadButton').attr('value', '<%= @boardingUser.employee_num %>');
  	}
	});
	
	
    $('#my-checkbox').change(function() {
        if($(this).is(":checked")) {
            $('#testing').hide();
            console.log("checked");
        }
        console.log('not checked');
        $('#testing').show();     
    });
});
</script>
<script type="text/javascript">
	// Set NumPad defaults for jQuery mobile. 
	// These defaults will be applied to all NumPads within this document!
	$.fn.numpad.defaults.gridTpl = '<table class="b-clear table modal-content"></table>';
	$.fn.numpad.defaults.backgroundTpl = '<div class="modal-backdrop in"></div>';
	$.fn.numpad.defaults.displayTpl = '<input type="password" class="form-control" />';
	$.fn.numpad.defaults.buttonNumberTpl =  '<button type="button" class="btn btn-default"></button>';
	$.fn.numpad.defaults.buttonFunctionTpl = '<button type="button" class="btn" style="width: 100%;"></button>';
	$.fn.numpad.defaults.onKeypadCreate = function(){$(this).find('.done').addClass('color btn btn-primary');};

	// Instantiate NumPad once the page is ready to be shown
	$(document).ready(function(){
		$('#text-basic').numpad();
		$('#password').numpad({
			displayTpl: '<input class="form-control" type="password" />',
			hidePlusMinusButton: true,
			hideDecimalButton: true	
		});
		$('#numpadButton-btn').numpad({
			target: $('#numpadButton')
		});
		$('#numpadButton-btn_2').numpad({
			target: $('#numpadButton_2')
		});
		$('#numpad4div').numpad();
		$('#numpad4column .qtyInput').numpad();
	
		$('#numpad4column tr').on('click', function(e){
		$(this).find('.qtyInput').numpad('open');
	});
});
</script>
<!-- Login Screen               --*>
<*-- Compiled and Error Checked --*>
<*-- On 9/27/2016               --->
<!--
<div class="row v-c">
	<div class="center card card-block card-shadow text-xs-center" >	
		<div class="container">
			<div class="left-login">
				<h2>Welcome to Watchdog</h2>
			</div>
			<div class="col-xs-7">
				<span class="right" style="margin-top: .25rem">
					<input type="checkbox" name="my-checkbox" checked >
				</span>
			</div>
		</div>
		<#%= form_tag '/login' do %>	
		<div class="container">
			<div class="m-t-2 row">
				<div class="right col-xs-4">          
					<#%= image_tag "badge.png", class:"img-fluid m-x-auto d-block" %>
				</div>
					<div class="col-xs-8 m-b-2">         
					<div class="left m-y-2 p-t-1 input-group">
						<#%= text_field_tag(:employee_num, nil, class: 'm-b-1 form-control', id: 'numpadButton', placeholder: "Employee Number")  %>
						<span class="input-group-btn">
							<button class="m-b-1 btn btn-default" id="numpadButton-btn" type="button"><i class="fa fa-th"></i></button>
						</span>  
					</div>
					<div class="left m-y-2 input-group">
						<#%= password_field_tag :password, nil, :class => "m-b-1 form-control", :id => "numpadButton_2", placeholder: "Password"%>
						<span class="input-group-btn">
							<button class="m-b-1 btn btn-default" id="numpadButton-btn_2" type="button"><i class="fa fa-th"></i></button>
						</span>            
					</div>
					<div class="right-login">
						<#%= submit_tag 'Login', class: 'color btn-size btn btn-primary' %>
					</div>					
				</div>
			</div>
		<#% end %>
		</div>
	</div>
</div>
<script>
	$("[name='my-checkbox']").bootstrapSwitch();
</script>
<script type="text/javascript">
	// Set NumPad defaults for jQuery mobile. 
	// These defaults will be applied to all NumPads within this document!
	$.fn.numpad.defaults.gridTpl = '<table class="b-clear table modal-content"></table>';
	$.fn.numpad.defaults.backgroundTpl = '<div class="modal-backdrop in"></div>';
	$.fn.numpad.defaults.displayTpl = '<input type="password" class="form-control" />';
	$.fn.numpad.defaults.buttonNumberTpl =  '<button type="button" class="btn btn-default"></button>';
	$.fn.numpad.defaults.buttonFunctionTpl = '<button type="button" class="btn" style="width: 100%;"></button>';
	$.fn.numpad.defaults.onKeypadCreate = function(){$(this).find('.done').addClass('color btn btn-primary');};

	// Instantiate NumPad once the page is ready to be shown
	$(document).ready(function(){
		$('#text-basic').numpad();
		$('#password').numpad({
			displayTpl: '<input class="form-control" type="password" />',
			hidePlusMinusButton: true,
			hideDecimalButton: true	
		});
		$('#numpadButton-btn').numpad({
			target: $('#numpadButton')
		});
		$('#numpadButton-btn_2').numpad({
			target: $('#numpadButton_2')
		});
		$('#numpad4div').numpad();
		$('#numpad4column .qtyInput').numpad();
	
		$('#numpad4column tr').on('click', function(e){
		$(this).find('.qtyInput').numpad('open');
	});
});
</script> -->